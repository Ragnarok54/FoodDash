@model FoodDash.Web.Models.Restaurant.RestaurantModel

@{
    ViewData["Title"] = Model.RestaurantId > 0 ? "Edit restaraunt in FoodDash" : "Add a new restaurant in FoodDash";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section style="padding:40px 0; background-position: center; background-repeat: no-repeat;background-size: cover; background-attachment:fixed;">
    <div class="container">
        <div class="textwidget">
            <i class="fa fa-star roundicon star"></i>
            <div class="contactstyle topform" style="margin-top: 2em">
                <h4 style="text-align: center">
                    @if (Model.RestaurantId > 0)
                    {
                        <text>Edit restaraunt in FoodDash</text>
                    }
                    else
                    {
                        <text>Add a new restaurant in FoodDash</text>
                    }
                </h4>
                <hr />
                <form asp-action="Edit" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <input asp-for="RestaurantId" hidden />

                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Description" class="control-label"></label>
                        <input asp-for="Description" class="form-control" />
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="TypeId" class="control-label"></label>
                        <select asp-for="TypeId" class="form-control" asp-items="ViewBag.RestaurantTypeId"></select>
                        <span asp-validation-for="TypeId" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="DeliveryTime" class="control-label"></label>
                        <input asp-for="DeliveryTime" class="form-control" />
                        <span asp-validation-for="DeliveryTime" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Photo" class="control-label"></label>
                        <input asp-for="Photo" class="form-control" type="file" style="padding: 0px; border: none; border-radius: 0px;"/>
                        <span asp-validation-for="Photo" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        @if (Model.RestaurantId > 0)
                        {
                            <input type="submit" value="Save changes" class="btn btn-primary" />
                            <input type="button" value="Delete restaurant" class="btn btn-danger" onclick="window.location.href='@Url.Action("Delete", "Restaurant", new { restaurantId = Model.RestaurantId})'" />
                        }
                        else
                        {
                            <input type="submit" value="Add" class="btn btn-primary" />
                        }
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
